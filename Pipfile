[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
black = "==19.3b0"

[packages]

[pipenv]
allow_prereleases = false

[requires]
python_version = "3.7"

[scripts]
check = "make check"
format = "make format"
test = "tox -p all"
